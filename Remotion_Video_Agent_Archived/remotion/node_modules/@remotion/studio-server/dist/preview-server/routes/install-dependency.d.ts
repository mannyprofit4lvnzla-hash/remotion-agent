import { type InstallPackageRequest, type InstallPackageResponse, type PackageManager } from '@remotion/studio-shared';
import type { ApiHandler } from '../api-types';
export declare const getInstallCommand: ({ manager, packages, version, }: {
    manager: PackageManager;
    packages: string[];
    version: string;
}) => string[];
export declare const handleInstallPackage: ApiHandler<InstallPackageRequest, InstallPackageResponse>;
